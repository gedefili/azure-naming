# Azure Naming Service IaC Moved

The Terraform for this service is no longer maintained in this repository.

The canonical IaC source was moved to the `environs-iac` repository at:

- `sanmar/applications/internal/azure-naming`

The active Terraform root for the service is:

- `sanmar/applications/internal/azure-naming/service`

Use that repository and path for all future planning, validation, and apply operations.

This `deploy/` directory is kept only as a relocation marker.
